#include <iostream>
#include <string>
#include <sstream>
#include <vector>
#include <bits/stdc++.h>

using namespace std;

int main() {
    string numberString;
    int n;
    //cin >> n;
    vector<string> a;
    for(int i = 0; i < 4; i++){
        cin >> a[i];
    }
    return 0;
}