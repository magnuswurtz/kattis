import java.io.BufferedReader;
import java.io.InputStreamReader;


/**
 * queens
 */
public class queens {

static boolean checkConfig(int i, int j){
//Given a position i,j check all fields that the queen can move.
//If there is a queen at one of the positions, return false and output "invalid"
return true;
}
    public static void main(String[] args) {
        BufferedReader in = new BufferedReader(new InputStreamReader(System.in));

        String[][] board = new String[8][8];
        //Read input and initialize board

        //go through board. For each queen check configuration.
        //If it returns true, continue to next queen, else quit
        
    }
}